# Testing dependencies for Pet Clinic CI/CD Pipeline
pytest>=7.0.0
boto3>=1.26.0
moto>=4.0.0
PyYAML>=6.0
requests>=2.28.0
cfn-lint>=0.77.0

# Property-based testing
hypothesis>=6.0.0

# Java testing (for later tasks)
# These will be used when we implement Java application tests
# jqwik (Java property testing) - will be added to Maven dependencies
# JUnit 5 - will be added to Maven dependencies